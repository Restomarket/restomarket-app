// Mock for better-auth/plugins module
export const bearer = jest.fn().mockReturnValue({});
export const admin = jest.fn().mockReturnValue({});
export const organization = jest.fn().mockReturnValue({});
